# Опционально: CUDA (PyTorch GPU) + insightface (распознавание лиц).
# Сначала всегда: pip install -r requirements_win.txt
# Потом при необходимости — пункты 1 и/или 2 ниже.
#
# --- 1. PyTorch с CUDA (для обучения на GPU) ---
# Одна команда (подставьте свою версию CUDA):
#
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
#
# Варианты: cu130 (CUDA 13.x), cu128, cu121, cu118.
# Проверка: python -c "import torch; print(torch.cuda.is_available())"
#
# --- 2. Insightface (распознавание лиц) ---
# Нужны Microsoft C++ Build Tools: https://visualstudio.microsoft.com/visual-cpp-build-tools/
# После установки Build Tools и перезапуска терминала:
#
#   pip install -r requirements_win_optional.txt
#
# (ниже только пакеты для пункта 2; пункт 1 — отдельная команда выше)

insightface==0.7.3
